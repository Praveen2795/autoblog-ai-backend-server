# Environment Configuration Template
# Copy this to .env and fill in your values

# ===========================================
# REQUIRED - Application will not work without these
# ===========================================

# Your Google Gemini API Key (get from https://aistudio.google.com/apikey)
GEMINI_API_KEY=your-gemini-api-key-here

# ===========================================
# SECURITY - Change these in production!
# ===========================================

# JWT Secret Key - generate with: openssl rand -hex 32
JWT_SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32

# ===========================================
# OPTIONAL - Defaults are provided
# ===========================================

# Application
APP_NAME=AutoBlog AI API
APP_VERSION=1.0.0
DEBUG=true

# CORS - Add your frontend URL(s)
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173","http://127.0.0.1:3000"]

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# Gemini Models
GEMINI_FLASH_MODEL=gemini-2.5-flash
GEMINI_PRO_MODEL=gemini-2.0-flash
GEMINI_IMAGE_MODEL=gemini-2.0-flash-exp

# Research Configuration
MAX_RESEARCH_RETRIES=5
MAX_REFINEMENT_ITERATIONS=5
QUALITY_THRESHOLD=90

# ===========================================
# EMAIL PIPELINE - For email-triggered blog generation
# ===========================================

# Your email address (Gmail recommended)
EMAIL_ADDRESS=your-email@gmail.com

# For Gmail: Use an App Password (NOT your regular password!)
# 1. Enable 2-Factor Authentication: https://myaccount.google.com/security
# 2. Create App Password: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and copy the 16-character password
EMAIL_PASSWORD=your-app-password-here

# IMAP Server (for receiving emails)
EMAIL_IMAP_SERVER=imap.gmail.com
EMAIL_IMAP_PORT=993

# SMTP Server (for sending emails)
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=465

# How often to check inbox for new requests (seconds)
EMAIL_CHECK_INTERVAL=60

# Auto-start email monitoring when server starts
EMAIL_AUTO_START=false

# SECURITY: Whitelist of allowed sender emails (comma-separated)
# Only emails FROM these addresses will trigger blog generation
# Leave empty to allow any sender (relies on spam filtering)
# RECOMMENDED: Set this to your own email(s) for security!
EMAIL_ALLOWED_SENDERS=your-email@gmail.com,your-other@email.com
